const data={project:{name:'Kentfield Ct',city:'Laconia',start:'05/09/2026',stage:'Insulation',status:'Em andamento'},labor:[{worker:'Carlos',stage:'Demolition',date:'05/09/2026',hours:8,rate:25,total:200},{worker:'Dênis',stage:'Demolition',date:'05/09/2026',hours:8,rate:25,total:200},{worker:'Welber',stage:'Demolition',date:'05/09/2026',hours:8,rate:25,total:200}],purchases:[{id:'PU001',vendor:'Home Depot',date:'05/09/2026',stage:'Demolition',amount:292.28,category:'A classificar',receipt:''}],sales:[{stage:'Drywall',amount:19000}],scope:'Limpeza final não faz parte do escopo'};
const usd=n=>n.toLocaleString('en-US',{style:'currency',currency:'USD'}); const labor=data.labor.reduce((a,x)=>a+x.total,0), purchases=data.purchases.reduce((a,x)=>a+x.amount,0), total=labor+purchases, hours=data.labor.reduce((a,x)=>a+x.hours,0);
const view=document.querySelector('#view'), crumbs=document.querySelector('#crumbs'); let path=[];
function go(...p){path=p;render()}; function c(label,...p){return `<button class="crumb" onclick='go(${p.map(x=>JSON.stringify(x)).join(',')})'>${label}</button>`}
function render(){crumbs.innerHTML=c('Visão geral')+(path.length?' › '+path.map((x,i)=>c(x,...path.slice(0,i+1))).join(' › '):''); const [a,b,c1]=path;
 if(!a){view.innerHTML=`<div class="grid"><article class="card"><div class="label">Etapa atual</div><div class="value">${data.project.stage}</div></article><article class="card click" onclick="go('Gastos')"><div class="label">Gasto até agora</div><div class="value">${usd(total)}</div><div class="note">Clique para detalhar</div></article><article class="card"><div class="label">Horas trabalhadas</div><div class="value">${hours} h</div></article><article class="card click" onclick="go('Vendas')"><div class="label">Valor vendido conhecido</div><div class="value">${usd(19000)}</div><div class="note">Drywall</div></article></div><h2 class="section-title" style="margin-top:24px">Atenção</h2><div class="row warning"><span>Compra Home Depot pendente de classificação</span><strong>${usd(purchases)}</strong></div>`;return}
 if(a==='Gastos'&&!b){view.innerHTML=`<h2 class="section-title">Gastos</h2><div class="list"><div class="row click" onclick="go('Gastos','Labor')"><span><strong>Labor</strong><div class="note">24 horas · 3 trabalhadores</div></span><span class="money">${usd(labor)}</span></div><div class="row click warning" onclick="go('Gastos','A classificar')"><span><strong>A classificar</strong><div class="note">Home Depot · material/consumível ou equipamento/ferramenta</div></span><span class="money">${usd(purchases)}</span></div><div class="row"><strong>Total</strong><span class="money">${usd(total)}</span></div></div>`;return}
 if(a==='Gastos'&&b==='Labor'&&!c1){view.innerHTML=`<h2 class="section-title">Labor por etapa</h2><div class="row click" onclick="go('Gastos','Labor','Demolition')"><span><strong>Demolition</strong><div class="note">24 horas</div></span><span class="money">${usd(labor)}</span></div>`;return}
 if(a==='Gastos'&&b==='Labor'&&c1==='Demolition'){view.innerHTML=`<h2 class="section-title">Demolition · trabalhadores</h2><div class="list">${data.labor.map(x=>`<div class="row click" onclick="go('Gastos','Labor','${x.worker}')"><span><strong>${x.worker}</strong><div class="note">${x.hours}h × ${usd(x.rate)}/h</div></span><span class="money">${usd(x.total)}</span></div>`).join('')}</div>`;return}
 if(a==='Gastos'&&b==='Labor'&&c1){const x=data.labor.find(y=>y.worker===c1);view.innerHTML=`<h2 class="section-title">Lançamento de labor</h2><article class="card detail"><strong>${x.worker}</strong><br>Etapa: ${x.stage}<br>Data: ${x.date}<br>Horas: ${x.hours}<br>Taxa: ${usd(x.rate)}/h<br><strong>Total: ${usd(x.total)}</strong></article>`;return}
 if(a==='Gastos'&&b==='A classificar'){const x=data.purchases[0];view.innerHTML=`<h2 class="section-title">Compra</h2><article class="card detail warning"><strong>${x.vendor}</strong><br>Data: ${x.date}<br>Etapa: ${x.stage}<br>Categoria: ${x.category}<br><strong>Total: ${usd(x.amount)}</strong><br>Comprovante: <span class="empty">link ainda não informado</span></article>`;return}
 if(a==='Vendas'){view.innerHTML=`<h2 class="section-title">Vendas conhecidas</h2><div class="row"><strong>Drywall</strong><span class="money">${usd(19000)}</span></div>`;return}}
render();
